# v1 → v2 schema capability disposition

**Date:** 2026-05-31 · **Related:** [[0005-schema-hardening]]

No silent capability loss (ADR-0005): every v1 object is KEEP / CHANGE / NEW / DROP / DEFER.

**Tally:** CHANGE=82, DEFER=34, DROP=8, KEEP=41, NEW=49

## auth

| v1 object | v2 object | Disposition | Note |
|---|---|---|---|
| governance.studio_role (enum, 10 members) | governance.platform_role (enum, 10 members) | **KEEP** | Renamed studio_role -> platform_role per spec; all 10 members carried verbatim (business_owner, compliance, legal, model_risk, ai_governance, security, privacy, engineer, auditor, viewer). lifecycle/CHECK conventions otherwise unchanged. |
| governance.approval_role (enum, 7 members, intake schema) | governance.platform_role (CHECK subset) | **CHANGE** | 7-member approval subset is no longer a parallel free-standing enum; modeled as the constrained subset of platform_role {business_owner,compliance,legal,model_risk,ai_governance,security,privacy}, enforced as a CHECK at sign-off sites (single source of truth, FR-009). The standalone enum is retired. |
| (none) — v1 had no persistent user table; identity was anonymous + cookie persona | governance.app_user | **NEW** | v2-new identity principal. Keyed on immutable (tenant_id, microsoft_oid) UNIQUE; UUIDv7 surrogate PK; display fields display-only; session_epoch + disabled_at for revocation/deactivation. Spec calls it `user`; hardened to `app_user` because `user` is a reserved word (naming-conventions §1) — see open issue. |
| (none) — v1 had no user->role persistence | governance.platform_role_grant | **NEW** | v2-new append-only platform-role grant/revoke event log; current state via governance.current_platform_role view; granted_by server-resolved (FR-017). |
| (none) — no v1 app-team dimension | governance.app_team_role_grant | **NEW** | v2-new append-only per-application role grant log scoped to application_id; current state via governance.current_app_team_role view (FR-010). |
| governance.app_team_role (enum) | governance.app_team_role (enum, 5 members) | **NEW** | v2-new enum (app_owner, app_lead, app_dev, app_sre, app_ops; renamed from app_demo_* — D-ONB-6). No v1 source. |
| (none) — no v1 auth audit log | governance.auth_event (+ auth_event_type, auth_event_outcome enums) | **NEW** | v2-new Tier-2 append-only auth audit log; month-range-partitioned on created_at, BRIN on time, no FK to app_user by design (hot-path/cross-tier avoidance). event_type/outcome hardened from spec free-text to enums. |
| web/middleware/persona.py cookie persona resolution | (API/middleware layer; not a schema object) | **CHANGE** | Role SOURCE changes cookie -> DB lookup against current_platform_role / current_app_team_role. Behavior (is_action_allowed, fail-closed, ~20-action matrix, least-privilege viewer default) carried verbatim in the application layer; no schema object for the matrix in this domain (matrix is code-level per spec). |
| persona_switcher.html (persona self-selection UI) | (removed) | **DROP** | Dropped with reason: replaced by Entra SSO real identity; no self-selection of persona. Recorded in spec capability-coverage table as DROP-with-reason. |
| v1 self-asserted persona attribution (acting_as_role, opened_by_role, signoff role, locked_role) | governance.app_user.app_user_id (FK attribution target) | **CHANGE** | Audit attribution now binds to the resolved immutable app_user_id of the authenticated principal instead of a self-selected cookie persona (FR-018). The downstream intake/approval columns themselves live in other domains; here we provide the identity target they bind to. |

## entities

| v1 object | v2 object | Disposition | Note |
|---|---|---|---|
| enum governance.lifecycle_state | governance.lifecycle_state | **KEEP** | 7 states verbatim: draft, candidate, staging, shadow, challenger, champion, deprecated. |
| enum governance.deployment_channel | governance.deployment_channel | **KEEP** | Verbatim members. |
| enum governance.materiality_tier | governance.materiality_tier | **KEEP** | high/medium/low verbatim. |
| enum governance.capability_type | governance.capability_type | **KEEP** | Verbatim. |
| enum governance.trust_level | governance.trust_level | **KEEP** | Verbatim; retained for downstream tool-trust use. |
| enum governance.data_classification | governance.data_classification | **KEEP** | Verbatim; used by tool.data_classification_max. |
| enum governance.entity_type | governance.entity_type | **CHANGE** | Members agent/task/prompt/tool plus intake-added test_suite/ground_truth_dataset folded into one CREATE TYPE (no silent loss of the ALTER additions). |
| enum governance.governance_tier | governance.governance_tier | **KEEP** | Verbatim. |
| enum governance.api_role | governance.api_role | **KEEP** | Verbatim. |
| enum governance.metric_type | governance.metric_type | **KEEP** | Verbatim. |
| enum governance.run_purpose | governance.run_purpose | **KEEP** | Verbatim. |
| agent_version.change_type (varchar20) | governance.version_change_type enum | **CHANGE** | Free-text promoted to enum (major/minor/patch). Applies to agent_version and task_version. |
| *_version.decision_log_detail (varchar20) | governance.decision_log_detail enum | **CHANGE** | Free-text 'standard' promoted to enum minimal/standard/verbose. |
| tool.transport (varchar50) | governance.tool_transport enum | **CHANGE** | Closed value set promoted to enum python_inprocess/mcp/http. |
| governance.model | governance.model | **CHANGE** | id->model_id; vendor string col model_id renamed provider_model_id; TIMESTAMPTZ retained; status CHECK added; uuidv7 PK. |
| governance.model_price | governance.model_price | **CHANGE** | id->model_price_id; named FK/uq; partial unique uq_model_price_active on open window kept. |
| governance.inference_config | governance.inference_config | **CHANGE** | id->inference_config_id; active->is_active; varchar->text; TIMESTAMP->timestamptz; named FK to model; range CHECKs on temperature/top_p/max_tokens; model_id FK folded in (was post-create ALTER). |
| governance.agent | governance.agent | **CHANGE** | id->agent_id; varchar->text; TIMESTAMP->timestamptz; current_champion_version_id now a real named FK (was soft pointer); idx_agent_name dropped (redundant with uq_agent_name). |
| governance.task | governance.task | **CHANGE** | id->task_id; same hardening as agent; current_champion_version_id now FK; idx_task_name dropped (redundant with unique). |
| governance.prompt | governance.prompt | **CHANGE** | id->prompt_id; primary_entity_id remains soft polymorphic pointer (validated by entity_type at API); updated_at folded in (was post-create ALTER). |
| governance.agent_version | governance.agent_version | **CHANGE** | id->agent_version_id; version_label generated col kept; named FKs incl cloned_from; uq renamed uq_agent_version_semver; traffic-pct CHECKs and temporal CHECK added; SCD-2 valid_from/valid_to retained. |
| governance.task_version | governance.task_version | **CHANGE** | Same hardening as agent_version. |
| governance.prompt_version | governance.prompt_version | **CHANGE** | id->prompt_version_id; version_label + test_required generated cols kept; named FKs; temporal CHECK; updated_at folded in. |
| governance.entity_prompt_assignment | governance.entity_prompt_assignment | **CHANGE** | id->surrogate; CHECK entity_type IN (agent,task) kept; entity_version_id remains soft polymorphic; named uq/idx. |
| governance.mcp_server | governance.mcp_server | **CHANGE** | id->mcp_server_id; active->is_active; varchar->text; named uq. |
| governance.tool | governance.tool | **CHANGE** | id->tool_id; mcp_server_name(text FK to name) replaced by mcp_server_id (FK to surrogate PK); transport->enum; active->is_active; is_write_operation/requires_confirmation NOT NULL; CHECK enforcing mcp pairing. |
| governance.data_connector | governance.data_connector | **CHANGE** | id->data_connector_id; varchar->text; named uq. |
| governance.agent_version_tool | governance.agent_version_tool | **CHANGE** | id->surrogate; authorized->is_authorized; named FK/uq; FK ON DELETE CASCADE from owning version; added ix on tool_id. |
| governance.task_version_tool | governance.task_version_tool | **KEEP** | Hardened (surrogate, named constraints). KEEP for no-silent-loss though binding-grammar makes tools agent-only; deprecation deferred — see open issues. |
| governance.source_binding | governance.source_binding | **CHANGE** | Source Binding retained name (already correct). owner_kind/binding_kind CHECKs replaced by enums binding_owner_kind/source_payload_kind; NEW source_kind enum (vault/task_output/structured) added per binding-grammar; required->is_required. |
| governance.write_target | governance.target_binding | **CHANGE** | RENAMED write_target->target_binding per binding-grammar. owner_kind->enum; NEW target_kind enum; connector_id->data_connector_id now nullable (structured/task_output need none) with CHECK requiring connector when target_kind=vault; name kept; required->is_required. |
| governance.target_payload_field | governance.target_payload_field | **CHANGE** | write_target_id->target_binding_id (FK to target_binding); id->surrogate; required->is_required; CASCADE retained. |
| governance.task_version_source | governance.task_version_source | **KEEP** | Hardened (id->surrogate, connector_id->data_connector_id FK named, required->is_required). KEPT for no-silent-loss; superseded by source_binding — retire deferred to migration. |
| governance.task_version_target | governance.task_version_target | **KEEP** | Hardened. KEPT for no-silent-loss; superseded by target_binding — retire deferred. |
| governance.agent_version_delegation | governance.agent_version_delegation | **CHANGE** | id->surrogate; authorized->is_authorized; chk_child_target rewritten as XOR CHECK; named FKs/uq/idx; parent FK CASCADE, child FK SET NULL. |
| governance.application | governance.application | **CHANGE** | id->application_id; varchar->text; named uq; TIMESTAMP->timestamptz. |
| governance.application_entity | governance.application_entity | **CHANGE** | id->surrogate; named FK CASCADE; entity_id remains soft polymorphic; named uq/idx. |
| view governance.entity_consumers | (deferred view) | **DEFER** | Reverse where-used view spans tables across this and runtime domains and references write_target (now target_binding). Re-author after all registry+runtime tables hardened; the FK edges it unions all exist here. |
| intake enum governance.studio_role | governance.platform_role | **CHANGE** | 10 values carried VERBATIM into platform_role per user-authentication.md. approval_role is the 7-value sign-off subset, modeled as a constraint not a parallel enum (handled in intake/approval domain). |
| (v2-new) account_user | governance.account_user | **NEW** | AUTH. v1 had no auth. Named account_user ('user' is reserved). Natural key (tenant_id, microsoft_oid); session_epoch; disabled_at fail-closed. |
| (v2-new) platform_role_grant | governance.platform_role_grant | **NEW** | AUTH append-only; revoke=new event; current_platform_role view over latest. Tier-1. |
| (v2-new) app_team_role_grant | governance.app_team_role_grant | **NEW** | AUTH append-only, per-application dimension (app_team_role enum, 5 values). Tier-1. |
| (v2-new) auth_event | governance.auth_event | **NEW** | AUTH append-only Tier-2; month range-partitioned on created_at; BRIN on time; no FK to account_user (cross-tier hot path). |
| (v2-new) harness_image | governance.harness_image | **NEW** | ADR-0006 harness image registry; digest-pinned (uq on repository+digest); tag advisory only. |
| (v2-new) package | governance.package | **NEW** | ADR-0006 package inventory (.vtx/.vax); CHECK ties package_kind to entity_type; digest unique. |
| (v2-new) package_harness_compatibility | governance.package_harness_compatibility | **NEW** | ADR-0006 declared package x image compatibility, digest-pinned; enforces refusal of incompatible deploys at API. |
| (v2-new) deployment | governance.deployment | **NEW** | ADR-0006 deployment inventory; append-only, lifecycle-gated, actor/target/outcome; environment CHECK. |
| (v2-new) run_dispatch_outbox | governance.run_dispatch_outbox | **NEW** | PCR 3.3 transactional outbox; partial index on unpublished rows for SKIP LOCKED relay; FK to runtime.execution_run added when runtime domain hardened. |

## lifecycle_approvals

| v1 object | v2 object | Disposition | Note |
|---|---|---|---|
| governance.lifecycle_state (enum) | governance.lifecycle_state (enum) | **KEEP** | 7 members verbatim: draft,candidate,staging,shadow,challenger,champion,deprecated. |
| governance.deployment_channel (enum) | governance.deployment_channel (enum) | **KEEP** | 5 members verbatim; used by lifecycle_event.channel and deployment context. |
| governance.materiality_tier (enum) | governance.materiality_tier (enum) | **KEEP** | Verbatim high/medium/low; captured on promotion. |
| agent_version.lifecycle_state / task_version.lifecycle_state / prompt_version.lifecycle_state (mutable column) | governance.lifecycle_event + view entity_lifecycle_current | **CHANGE** | v1 mutated a column in place; v2 makes each transition an append-only event, current state is a VIEW (ADR-0005 rule 3). Version-table columns themselves are owned by the entity-model domain. |
| agent.current_champion_version_id (no-FK mutable UUID) | governance.champion_assignment + view entity_champion_current | **CHANGE** | v1 soft pointer with NO FK, updated in place; v2 is an append-only assignment log with FK to promotion, current champion is a VIEW. |
| task.current_champion_version_id (no-FK mutable UUID) | governance.champion_assignment + view entity_champion_current | **CHANGE** | Same as agent: unified into one polymorphic champion_assignment table keyed by (entity_type, entity_id). |
| governance.approval_record (table) | governance.promotion | **CHANGE** | v1 approval_record was the lifecycle gate / champion-confirmation record. Re-expressed as the promotion-attestation event: all gate-review flags + champion_confirmation_satisfied + decision_override/override_reason carried verbatim; from_state/to_state kept; approver_name/approver_role -> promoted_by_user_id (FR-018). hitl_approval_id back-reference (runtime.agent_decision_log) is satisfied by referencing approval_request instead. |
| approval_record.gate_type (varchar) | governance.approval_request.kind (enum) + governance.lifecycle_event | **CHANGE** | Free-text gate_type subsumed by approval_request_kind and the lifecycle transition (from_state/to_state) it gates. |
| approval_record.champion_confirmation_satisfied (post-create column) | governance.promotion.champion_confirmation_satisfied | **KEEP** | Carried verbatim as a NOT NULL DEFAULT false attestation flag. |
| governance.approval_request (table, intake) | governance.approval_request | **CHANGE** | status varchar(20) -> approval_request_status enum; ai_risk_tier added (drives required-role set); required_roles jsonb DROPPED (derived from ai_risk_tier per v1 REQUIRED_ROLES_BY_RISK_TIER); opened_by/opened_by_role -> opened_by_user_id (FR-018); decided_at invariant + target pair check added; FK to intake kept (CASCADE). |
| approval_request.required_roles (jsonb) | (derived from ai_risk_tier) | **DROP** | DROP-with-reason: required role set is a pure function of ai_risk_tier (v1 intake.py REQUIRED_ROLES_BY_RISK_TIER), not per-row data. Storing it invited drift; computed at the API layer from the captured ai_risk_tier. |
| approval_request.target_entity_type / target_entity_id (polymorphic, no FK) | governance.approval_request.target_entity_type / target_entity_id | **KEEP** | Kept polymorphic (no DB FK) as in v1; paired-null CHECK added; entity_type narrowed to versioned_entity_type enum. App-validated. |
| governance.approval_signoff (table) | governance.approval_signoff | **CHANGE** | approver_email natural key -> approver_user_id (FR-018: real identity, not self-asserted); UNIQUE(request,role,email) -> UNIQUE(request,role,user_id); role kept as 7-member approval_role enum; append-only audit fact; FK CASCADE kept. |
| governance.approval_role (enum) | governance.approval_role (enum) | **KEEP** | 7 members verbatim; the sign-off subset of platform_role (auth domain owns platform_role). |
| governance.approval_decision (enum) | governance.approval_decision (enum) | **KEEP** | approved/rejected/requested_changes/abstained verbatim. |
| governance.approval_request_kind (enum) | governance.approval_request_kind (enum) | **KEEP** | intake/risk_reclassification/promote_candidate/promote_champion/retire verbatim. |
| governance.ai_risk_tier (enum) | governance.ai_risk_tier (enum) | **KEEP** | minimal/limited/high/unacceptable verbatim; now captured on approval_request to drive required-role sets. |
| governance.intake_artifact_plan_estimate (table) | governance.intake_artifact_plan_estimate | **CHANGE** | PK gen_random_uuid->uuidv7; created_by/acting_as_role -> created_by_user_id (FR-018); varchar(60) scenario_label -> text; FK to model(model_id) and plan kept; chk_estimate_override_pair + partial unique active index kept. |
| governance.intake_roi_assessment (table) | governance.intake_roi_assessment | **CHANGE** | PK->uuidv7; locked_by/locked_role -> locked_by_user_id, created_by/acting_as_role -> created_by_user_id (FR-018); lock-pair CHECK ((locked_at IS NULL)=(locked_by_user_id IS NULL)); ai_spend_basis varchar->text; FK to approval_request RESTRICT; partial unique active index kept. All ~40 financial numeric columns preserved verbatim with precision/scale. |
| governance.intake_cost_envelope (table) | governance.intake_cost_envelope | **CHANGE** | PK->uuidv7; locked_by/locked_role -> locked_by_user_id (FR-018); inline UNIQUE(intake_id) -> named uq constraint; FK to approval_request RESTRICT; upside_pct>=0 CHECK added. |
| governance.source_binding (table) | (retired -> Source Binding in entity-model domain) | **DEFER** | Binding-grammar rename source_binding -> Source Binding is owned by the entity-model/composition domain, not lifecycle. Noted here only to confirm no lifecycle table references the v1 name. |
| governance.write_target (table) | (retired -> Target Binding in entity-model domain) | **DEFER** | write_target -> Target Binding rename owned by the entity-model domain. The .vtx/.vax bundle's write_targets.json (PCR 3.2) references Target Bindings; package contents are not modeled per-binding here. |
| analytics.v_lifecycle_event (view, from approval_record) | governance.lifecycle_event + governance.entity_lifecycle_current | **CHANGE** | v1 derived lifecycle events from approval_record gate rows. v2 has a first-class lifecycle_event table; the analytics view is re-pointed at it (analytics layer owned elsewhere). |
| analytics.v_intake_approval (view) | (re-point onto hardened approval_request/approval_signoff) | **DEFER** | Analytics mart view; re-derives from the hardened tables. Owned by the compliance/analytics-views domain; columns map 1:1 except email->user_id resolution. |
| analytics.v_intake_active_estimate / v_intake_cost_drift (views) | (re-point onto hardened estimate/envelope) | **DEFER** | Phase-D mart views over plan_estimate/cost_envelope; re-derive from hardened tables (column shapes preserved). Owned by analytics-views domain. |
| governance.harness_image | governance.harness_image | **NEW** | V2-NEW (ADR-0006 §2): digest-pinned harness image registry; tag-based compat prohibited (sha256: CHECK). |
| governance.package | governance.package | **NEW** | V2-NEW (ADR-0006/PCR 3.2): .vtx/.vax artifact built at champion promotion; manifest_digest SHA-256 integrity anchor; package_kind<->entity_type CHECK. |
| governance.package_image_compatibility | governance.package_image_compatibility | **NEW** | V2-NEW (ADR-0006 §2): M:N digest-pinned compatibility; deploy gate refuses absent combos. |
| governance.cluster | governance.cluster | **NEW** | V2-NEW (ADR-0006 §1): cluster registry grouped into environment_class for placement gating. |
| governance.deployment_event | governance.deployment_event + view deployment_current | **NEW** | V2-NEW (ADR-0006 §3): append-only deployment inventory (actor/target/outcome), lifecycle-gated placement via CHECKs; current placement is a VIEW. |
| governance.promotion (attestation event) | governance.promotion | **NEW** | V2-NEW shape (PCR 3.2): captures package digest + inference_config snapshot + promoted_by + attestation flags at champion promotion. Subsumes v1 approval_record's promotion semantics (see approval_record CHANGE). |
| governance.run_dispatch_outbox | governance.run_dispatch_outbox | **NEW** | V2-NEW (PCR 3.3): transactional outbox; one row per execution_run inserted in the same txn; relay publishes pending via SKIP LOCKED; partial index on status='pending'. |
| governance.lifecycle_event (champion linkage) | governance.champion_assignment | **NEW** | V2-NEW append-only champion log replacing the two no-FK soft pointers; FK to promotion; retirement events supported. |

## compliance

| v1 object | v2 object | Disposition | Note |
|---|---|---|---|
| compliance.regulatory_framework | compliance.regulatory_framework | **KEEP** | Carried over verbatim. id->regulatory_framework_id uuidv7; gen_random_uuid()->uuidv7(); named pk/uq/ck constraints. Bitemporal validity (valid_from/valid_to) preserved. Tier-1. |
| compliance.regulatory_provision | compliance.regulatory_provision | **KEEP** | id->regulatory_provision_id; framework_id->regulatory_framework_id; column 'text' renamed provision_text (reserved-word hygiene per conventions sec.1). FK ON DELETE RESTRICT preserved. embedding columns DEFERred (see embedding_config row). |
| compliance.canonical_requirement_theme | compliance.governance_domain | **CHANGE** | Renamed to governance_domain. ADR-0008 organizes requirements by governance DOMAIN and computes per-domain maturity; v1 'theme' was grouping-only. Same code/name/description shape; gains maturity-scoring role via domain_maturity. |
| compliance.canonical_requirement | compliance.canonical_requirement | **KEEP** | Stable technology-agnostic vocabulary kept (ADR-0008 'requirements need not change much'). theme_id->governance_domain_id FK. id->canonical_requirement_id. embedding columns DEFERred. |
| compliance.provision_requirement_map | compliance.provision_requirement | **CHANGE** | Renamed; ADDS min_tier_level (the minimum cumulative tier a provision demands). match_strength/confidence preserved with CHECKs; mapping_source CHECK promoted to enum compliance.mapping_source (members verbatim). Bridge 1. |
| compliance.feature_plane | (none) | **DROP** | DROP-with-reason (ADR-0008): the canonical->feature axis is replaced by canonical->control+evidence. Examiners consume controls/evidence, not the product feature list. |
| compliance.feature_capability | (none) | **DROP** | DROP-with-reason: feature hierarchy level 2; replaced by control/evidence_specification right axis. |
| compliance.feature | (none) | **DROP** | DROP-with-reason: feature hierarchy level 3; replaced by control/evidence_specification. feature.status enum dropped with it. |
| compliance.requirement_feature_link | compliance.requirement_control | **DROP** | DROP-with-reason and superseded: the requirement<->feature bridge is replaced by requirement_control (Bridge 2), wiring requirement_tier->control/evidence_spec per tier per phase. role enum (primary/supporting) dropped. |
| (none) | compliance.requirement_tier | **NEW** | v2-NEW cumulative tier ladder per canonical requirement (tier N implies 1..N active). Variable depth. UNIQUE(canonical_requirement_id, tier_level). |
| (none) | compliance.control | **NEW** | v2-NEW right axis: control with control_type, phase (design_time/deploy_time/static_model/execution), enforcement_action. Three new enums. |
| (none) | compliance.evidence_specification | **NEW** | v2-NEW: artifact_type/produced_by/citable_as spec per control; what evidence a control must produce. |
| (none) | compliance.evidence | **NEW** | v2-NEW append-only audit fact tied to requirement+tier+phase+entity/run. Tier-2: range-partitioned monthly on created_at, BRIN. PK(evidence_id, created_at) for partition pruning. No UPDATE/DELETE. |
| (none) | compliance.exception | **NEW** | v2-NEW append-only first-class exception: waived tier, requirement, named approver (auth-linked), compensating controls, expiry. Status events appended; exception_current view projects latest. CHECKs enforce approver+expiry on 'approved'. |
| (none) | compliance.requirement_control | **NEW** | v2-NEW Bridge 2: requirement_tier<->control(+evidence_spec) per tier per phase. UNIQUE(tier, control, phase). |
| (none) | compliance.domain_maturity | **NEW** | v2-NEW per-domain normalized maturity score snapshots (append-only); domain_maturity_current view. Normalization algorithm deferred to compliance component spec. |
| compliance.requirement_coverage | (deferred / analytics-mart) | **DEFER** | Analytics-mart coverage vocab. coverage_level enum is preserved in this domain's DDL for reuse, but the requirement_coverage TABLE belongs to the analytics-mart domain and is hardened there, not here. No silent loss: vocabulary retained. |
| compliance.embedding_config | (deferred) | **DEFER** | Embedding model registry + the embedding/embedding_model_id columns on provision/requirement (and mart_field) are a cross-cutting vector-search concern, not part of the control/evidence metamodel. DEFER to a vector/embedding domain; provision and canonical_requirement omit embedding columns here. Re-attach as nullable FK when that domain lands. |
| analytics.mart_field | (deferred / analytics-mart) | **DEFER** | Analytics-mart vocabulary; preserved (semantic_type vocab intact) but hardened in the analytics-mart domain, out of scope for the control/evidence metamodel. Listed for no-silent-loss. |
| analytics.feed_view | (deferred / analytics-mart) | **DEFER** | Analytics view allowlist; preserve in analytics-mart domain. Out of scope here. |
| compliance.requirement_evidence_field | (deferred / analytics-mart) | **DEFER** | L4 semantic layer mapping canonical_requirement->mart_field. Preserve in analytics-mart domain. NOTE name clash with new evidence model: this is analytics 'evidence fields', distinct from compliance.evidence audit facts. |
| compliance.report_definition | (deferred / analytics-mart) | **DEFER** | L5 reports-as-data; preserve report_kind/output vocab in analytics-mart domain. Out of scope here. |
| compliance.report_requirement | (deferred / analytics-mart) | **DEFER** | L5 report<->canonical_requirement bridge; preserve in analytics-mart domain. |
| compliance.report_field_override | (deferred / analytics-mart) | **DEFER** | L5 per-report field override; preserve in analytics-mart domain (role/aggregation vocab intact). |
| compliance.report_sql_template | (deferred / analytics-mart) | **DEFER** | L5 BYO-SQL escape hatch; preserve in analytics-mart domain. |
| compliance.report_run_log | (deferred / analytics-mart, Tier-2) | **DEFER** | L5 report run audit trail; preserve in analytics-mart domain as a Tier-2 append-only log. Out of scope for control/evidence metamodel. |
| analytics.v_* compliance views (12, compliance_views inventory) | (deferred / analytics-mart) | **DEFER** | L2 logical mart views (v_entity_version, v_decision, v_intake, etc.) read from governance/runtime/intake L1; they are part of the analytics-mart layer, rebuilt once those domains are hardened. Not redefined in this domain. exception_current/domain_maturity_current are the only views this domain owns. |
| provision_requirement_map.mapping_source CHECK | compliance.mapping_source enum | **CHANGE** | Inline CHECK(IN ...) promoted to pg enum per conventions sec.9; members verbatim (manual/semantic_recommended/human_validated). |
| requirement_coverage.coverage_level CHECK | compliance.coverage_level enum | **KEEP** | Vocabulary preserved as enum (full/substantial/partial/gap) for analytics-mart reuse, members verbatim. |

## intake

| v1 object | v2 object | Disposition | Note |
|---|---|---|---|
| governance.application | governance.application | **CHANGE** | PK id->application_id uuidv7; varchar->text; added updated_at + named uq_application_name, ck name-not-blank. Tier-1. |
| governance.application_entity | (registry domain) governance.application_entity | **DEFER** | Application-to-entity membership bridge belongs to the REGISTRY/core domain, not INTAKE. Carried there (KEEP/CHANGE); flagged so it is not silently dropped. |
| governance.intake | governance.intake | **CHANGE** | PK id->intake_id uuidv7; gen_random_uuid->uuidv7; varchar->text; named FK fk_intake_application (RESTRICT); named uq_intake_application_code; added CHECKs (code-not-blank, approved/retired-at consistency, affected_populations is array). All enums verbatim. |
| governance.intake_impact_assessment | governance.intake_impact_assessment | **CHANGE** | PK->intake_impact_assessment_id uuidv7; added updated_at + created_at; named uq (intake_id,version); jsonb-array CHECKs; positive-version CHECK; FK CASCADE preserved; added missing index on intake_id. |
| governance.intake_requirement | governance.intake_requirement | **CHANGE** | PK->intake_requirement_id uuidv7; added created_at; vector(384)+ivfflat preserved; self-ref parent FK SET NULL + anti-self-parent CHECK; embedding_model_id FK retargeted to governance.embedding_config(embedding_config_id) (compliance->governance namespace in v2); FK index added. |
| governance.intake_entity_link | governance.intake_entity_link | **CHANGE** | PK->intake_entity_link_id uuidv7; named uq preserved; ADDED the commented-but-missing partial unique uq_intake_entity_link_no_requirement for NULL requirement_id. entity_id stays polymorphic (app-validated, documented). |
| governance.intake_artifact_plan | governance.intake_artifact_plan | **CHANGE** | PK->intake_artifact_plan_id uuidv7; jsonb object CHECKs on proposed_inputs/outputs; realized-when-realized CHECK; named uq preserved; realized_entity_id stays polymorphic (no DB FK). |
| governance.intake_artifact_plan_estimate | governance.intake_artifact_plan_estimate | **CHANGE** | PK->intake_artifact_plan_estimate_id uuidv7; FK plan_id renamed intake_artifact_plan_id; proposed_model_id FK->governance.model(model_id) SET NULL; override-pair CHECK preserved (renamed ck_*); partial-unique active scenario preserved; peak>0 CHECK added. |
| governance.approval_request | governance.approval_request | **CHANGE** | PK->approval_request_id uuidv7; status varchar->NEW enum governance.approval_request_status; required_roles jsonb-array CHECK; decided-when-terminal + target-pair CHECKs; added updated_at; target_entity_id polymorphic (no DB FK). |
| governance.approval_signoff | governance.approval_signoff | **CHANGE** | PK->approval_signoff_id uuidv7; reclassified as Tier-1 APPEND-ONLY audit fact (no updated_at; corrections are new rows); named uq (request,role,email) + FK index preserved. |
| governance.intake_roi_assessment | governance.intake_roi_assessment | **CHANGE** | PK->intake_roi_assessment_id uuidv7; all numeric precisions preserved verbatim; approval_request_id FK SET NULL; partial-unique active per intake preserved; locked-pair + horizon>0 CHECKs added; ai_spend_basis varchar->text. |
| governance.intake_cost_envelope | governance.intake_cost_envelope | **CHANGE** | PK->intake_cost_envelope_id uuidv7; inline UNIQUE(intake_id) -> named uq_intake_cost_envelope_intake; approval_request_id FK SET NULL; added created_at/updated_at; non-negative amounts CHECK. |
| governance.intake_status (enum) | governance.intake_status | **KEEP** | Members verbatim. Distinct from asset lifecycle_state (draft..deprecated); intake workflow states retained as-is. |
| governance.ai_risk_tier (enum) | governance.ai_risk_tier | **KEEP** | Verbatim: minimal/limited/high/unacceptable. |
| governance.naic_materiality (enum) | governance.naic_materiality | **KEEP** | Verbatim: material/non_material. |
| governance.requirement_kind (enum) | governance.requirement_kind | **KEEP** | Verbatim. |
| governance.requirement_status (enum) | governance.requirement_status | **KEEP** | Verbatim. |
| governance.requirement_relationship (enum) | governance.requirement_relationship | **KEEP** | Verbatim. |
| governance.studio_role (enum) | governance.studio_role | **KEEP** | Verbatim 10 members. Distinct from auth platform/app roles (ADR user-authentication); studio acting-as roles retained. |
| governance.approval_role (enum) | governance.approval_role | **KEEP** | Verbatim 7 members. |
| governance.approval_decision (enum) | governance.approval_decision | **KEEP** | Verbatim. |
| governance.approval_request_kind (enum) | governance.approval_request_kind | **KEEP** | Verbatim. |
| governance.artifact_plan_status (enum) | governance.artifact_plan_status | **KEEP** | Verbatim. |
| governance.approval_request.status (free varchar) | governance.approval_request_status (enum) | **NEW** | v1 status was free-text varchar(20) default 'pending'. Promoted to named enum (pending/approved/rejected/cancelled) per ADR-0005 (closed set => enum). |
| governance.entity_type ALTER (+test_suite,+ground_truth_dataset) | governance.entity_type | **DEFER** | Base entity_type enum + its members are owned by the REGISTRY/core domain. INTAKE references it (intake_entity_link, artifact_plan, approval_request). The two added members must be carried in that domain's enum definition; flagged here so not silently lost. |
| governance.intake_canonical_link (removed in v1) | governance.intake_obligation_resolution + governance.intake_obligation + view intake_obligation_current | **NEW** | v1 deleted intake_canonical_link because compliance attached to capability/feature. ADR-0008 reverses that: intake carries a resolved obligation-set linkage to canonical_requirements (by domain + target tier). Append-only resolution + immutable obligation rows + current-state view. FR-IN-014. |
| compliance.embedding_config | governance.embedding_config | **DEFER** | Referenced by intake_requirement.embedding_model_id. Owned by the COMPLIANCE domain; v2 moves it into governance schema (embedding_config_id PK). FK declared here; table hardened in compliance domain. |
| compliance.canonical_requirement | governance.canonical_requirement | **DEFER** | Center axis of ADR-0008 model; owned by COMPLIANCE domain. INTAKE's intake_obligation FKs to it. Hardened there. |
| governance.model (table) | governance.model | **DEFER** | Referenced by intake_artifact_plan_estimate.proposed_model_id. Owned by REGISTRY/model domain. FK declared; table hardened there. |

## decisions

| v1 object | v2 object | Disposition | Note |
|---|---|---|---|
| runtime.agent_decision_log (table) | governance.agent_decision_log | **CHANGE** | Moved runtime->governance. Tier-2 append-only, RANGE-partitioned monthly on created_at + BRIN. PK now (agent_decision_log_id, created_at) (partition-key requirement). id->agent_decision_log_id uuidv7. status varchar->decision_status enum; decision_log_detail varchar->enum. CHECK(entity_type IN agent/task/tool) named. v4 default->uuidv7(). All self/run/approval FKs demoted to documented soft refs (Tier-2 not an FK target). |
| runtime.agent_decision_log.source_resolutions (col) | governance.agent_decision_log.source_binding_resolutions | **CHANGE** | Renamed per binding-grammar (Source Binding). jsonb capture retained. |
| runtime.agent_decision_log.target_writes (col) | governance.agent_decision_log.target_binding_writes | **CHANGE** | Renamed per binding-grammar (Target Binding, retiring write_target lineage). jsonb capture retained. |
| runtime.agent_decision_log.status/error_message/decision_log_detail (cols) | governance.agent_decision_log.status/error_message/decision_log_detail | **CHANGE** | status->decision_status enum, decision_log_detail->enum; ck_..._error_requires_message added (error => error_message NOT NULL). |
| runtime.model_invocation_log (table) | governance.model_invocation_log | **CHANGE** | Moved runtime->governance. Tier-2 append-only, monthly RANGE partition + BRIN. PK (model_invocation_log_id, created_at). status varchar->invocation_status enum. cache token cols NOT NULL DEFAULT 0. CHECK completed>=started, tokens>=0, api_call_count>=1. v1 FK decision_log_id ON DELETE CASCADE DROPPED -> soft ref (append-only logs never deleted); model_id FK -> soft ref to decouple Tier-2 write path. |
| runtime.hitl_override (table) | governance.hitl_override | **CHANGE** | Moved runtime->governance. Tier-1 append-only audit fact. id->hitl_override_id uuidv7. v1 entity_type(varchar)->business_entity_type (kept free text, NOT registry enum). created_by varchar->uuid soft ref to governance.user. v1 FK decision_log_id ON DELETE CASCADE DROPPED -> soft ref (parent is Tier-2/partitioned; overrides immutable). Indexes renamed ix_*. |
| governance.model (table) | governance.model | **CHANGE** | KEEP Tier-1. id->model_id uuidv7; v1 natural col model_id->provider_model_id (avoids clash with surrogate convention). status varchar->model_status enum. uq_model->uq_model_provider_model. Indexes renamed. ck context_window>0. |
| governance.model_price (table) | governance.model_price | **CHANGE** | KEEP Tier-1, explicit SCD-2 append-only. id->model_price_id uuidv7. currency varchar(3)->currency_code enum. Named CHECKs: window valid_to>valid_from, all prices >=0. Partial UNIQUE uq_model_price_open_per_model (one open row/model). ix_model_price_model_valid_from. Cost still computed point-in-time, never stored. |
| analytics.v_model_invocation_cost (view) | governance.v_model_invocation_cost | **CHANGE** | KEEP semantics: prices each invocation by the model_price window containing started_at; computes input/output/cache_write/cache_read/total. Moved analytics->governance; column names updated to v2 (model_invocation_log_id). LEFT JOIN so unpriced calls still surface. |
| governance.run_purpose (enum) | governance.run_purpose | **KEEP** | Verbatim: production, test, validation, audit_rerun. |
| governance.entity_type (enum, decision-log usage) | governance.entity_type (referenced) | **KEEP** | Enum owned by registry domain; decision log restricts to agent/task/tool via named CHECK. Not redefined here. |
| decision_log status free varchar (implicit vocab) | governance.decision_status (enum) | **NEW** | Hardened v1 free-text status to enum complete/error/partial. |
| decision_log_detail free varchar (implicit vocab) | governance.decision_log_detail (enum) | **NEW** | Hardened v1 free-text 'standard' to enum minimal/standard/full. Also used by agent_version/task_version in registry domain (coordinate there). |
| model_invocation status free varchar | governance.invocation_status (enum) | **NEW** | Hardened to complete/error/timeout. |
| model.status free varchar | governance.model_status (enum) | **NEW** | Hardened to active/deprecated/retired. |
| model_price.currency varchar(3) | governance.currency_code (enum) | **NEW** | Hardened ISO-3 free text to enum usd/eur/gbp (extend additively). |
| runtime.execution_run_completion.decision_log_id FK | (soft ref; defined in run domain) | **DEFER** | Belongs to the run state-machine domain. v1 FK -> agent_decision_log must become a soft ref there because the decision log is now Tier-2/partitioned. Flagged for that domain owner. |
| runtime.execution_run_error.decision_log_id FK | (soft ref; defined in run domain) | **DEFER** | Same as above; FK into Tier-2 partitioned log not enforceable — soft ref in run domain. |
| runtime.agent_decision_log.hitl_approval_id FK -> governance.approval_record(id) | governance.agent_decision_log.hitl_approval_id (soft ref) | **CHANGE** | approval_record is being reworked into the compliance control/evidence + auth approval model (ADR-0008); kept as soft uuid ref pending that domain's final PK. Flagged in open_issues. |

## runs_quotas

| v1 object | v2 object | Disposition | Note |
|---|---|---|---|
| runtime.execution_run | runtime.execution_run | **CHANGE** | PK id->execution_run_id (uuidv7). entity_kind VARCHAR+CHECK -> runtime.run_entity_kind enum. write_mode free-text -> runtime.run_write_mode enum (live/read_only) wiring PCR 3.7. enforce_output_schema now NOT NULL DEFAULT true. submitted_* timestamps -> timestamptz. FK to execution_context renamed fk_execution_run_context. Indexes renamed ix_*. Still append-only Tier-1 submission record. |
| runtime.execution_run_status | runtime.execution_run_status | **CHANGE** | PK id->execution_run_status_id. status VARCHAR+CHECK('submitted','claimed','heartbeat','released') -> runtime.run_status enum (members verbatim). recorded_at -> created_at timestamptz. Added ck_*_worker_when_claimed invariant and jsonb detail. Append-only event table per naming-conventions §7. |
| runtime.execution_run_completion | runtime.execution_run_completion | **CHANGE** | PK id->execution_run_completion_id. final_status VARCHAR+CHECK -> runtime.run_completion_status enum. UNIQUE(execution_run_id) named uq_execution_run_completion_run. decision_log_id FK retargets runtime.agent_decision_log(agent_decision_log_id). Added duration nonneg check. Append-only terminal fact. |
| runtime.execution_run_error | runtime.execution_run_error | **CHANGE** | PK id->execution_run_error_id. UNIQUE(execution_run_id) named uq_execution_run_error_run. failed_at timestamptz. decision_log_id FK retargets agent_decision_log surrogate. error_message remains NOT NULL. Append-only terminal fact; written pre-side-effects on INTEGRITY_VIOLATION (PCR 3.2). |
| analytics.execution_run_current (view) | runtime.execution_run_current (view) | **CHANGE** | Moved analytics->runtime (co-located with the run state machine it projects). Same precedence resolution (completion->failed-if-error->latest status->submitted). Now exposes PCR 3.6 columns explicitly: current_status, submitted_at, first_started_at, current_worker_id, duration_ms (computed when not stored). |
| governance.quota | governance.quota | **CHANGE** | PK id->quota_id (uuidv7). scope_type/period VARCHAR+CHECK -> quota_scope_type/quota_period enums. V2-NEW configurable enforcement_action enum (alert_only/block/throttle); v1 boolean hard_stop retained as GENERATED mirror (hard_stop == enforcement_action='block'). Added ck_quota_budget_positive. Indexes renamed ix_*. |
| governance.quota_check | governance.quota_check | **CHANGE** | PK id->quota_check_id. alert_level VARCHAR -> quota_alert_level enum. Added ck_quota_check_period_order, spend nonneg, and alert_level-when-fired invariants. FK quota_id CASCADE preserved (named fk_quota_check_quota). Append-only evaluation record; resolved_at is a bounded operational close. |
| runtime.execution_run.write_mode (VARCHAR) | runtime.run_write_mode enum | **CHANGE** | Free-text write_mode promoted to enum live/read_only, encoding PCR 3.7 Target-Binding suppression for shadow/challenger/deprecated environments. |
| (none) | runtime.run_dispatch_outbox | **NEW** | V2-NEW transactional outbox (PCR 3.3). Inserted in the same txn as execution_run; relay publishes pending rows via SKIP LOCKED to NATS verity.runs.pending and advances status; sweep re-publishes stuck rows. Partial indexes for relay/sweep hot paths. Tier-1 dispatch plumbing. |
| (none) | governance.quota_enforcement_action enum | **NEW** | V2-NEW configurable enforcement vocabulary (alert_only/block/throttle) replacing the v1 single boolean; required by the brief's 'v2 configurable hard_stop enforcement flag'. |
| runtime.execution_context | runtime.execution_context | **DEFER** | FK target of execution_run; execution_context belongs to the runtime-context/application domain and is hardened there. Referenced here as fk_execution_run_context -> runtime.execution_context(execution_context_id) (assumed v2 surrogate PK name). |
| runtime.agent_decision_log | runtime.agent_decision_log | **DEFER** | FK target of execution_run_completion/error decision_log_id; agent_decision_log is a Tier-2 bulk/audit log hardened in the DECISION-LOG/ANALYTICS domain. Referenced here as fk_*_decision -> runtime.agent_decision_log(agent_decision_log_id) (assumed v2 surrogate PK name). |
| governance.deployment_channel (enum) | governance.deployment_channel (enum) | **DEFER** | Used by execution_run.channel; the deployment_channel enum is owned/defined by the registry/lifecycle domain. KEEP verbatim there; referenced here only. |
| runtime.model_invocation_log | runtime.model_invocation_log | **DEFER** | High-volume Tier-2 bulk log; NOT in this domain. Hardened (partitioned + BRIN) in the DECISION-LOG/ANALYTICS domain per ADR-0004/0007. The naming-conventions doc's worked example is illustrative only. |
| runtime.hitl_override | runtime.hitl_override | **DEFER** | Override records are Tier-1 run-adjacent but belong to the HITL/decision domain (FK to agent_decision_log). Not hardened here. |

## packages_deploy

| v1 object | v2 object | Disposition | Note |
|---|---|---|---|
| (none) | governance.harness_image | **NEW** | v2-new per ADR-0006 §2. Digest-identified Verity harness image registry; Tier-1 insert-only. No v1 equivalent (v1 had no harness-image governance). |
| (none) | governance.package | **NEW** | v2-new per ADR-0006. .vtx/.vax package inventory pinned to a source agent_version/task_version, identified by immutable digest; Tier-1 insert-only. No v1 equivalent. |
| (none) | governance.package_harness_image | **NEW** | v2-new per ADR-0006 §2. Digest-pinned package x harness-image compatibility bridge; the deploy path refuses non-listed combos. Tier-1 insert-only. |
| (none) | governance.deployment_environment | **NEW** | v2-new per ADR-0006 §1. Environment registry (non_prod/prod/ephemeral). Note: distinct from v1 governance.deployment_channel enum which described run channel on the entity_version, not a deployable environment. See DEFER note on deployment_channel. |
| (none) | governance.deployment_cluster | **NEW** | v2-new per ADR-0006 §1. Target-cluster registry incl. ephemeral replay clusters. No v1 equivalent. |
| (none) | governance.deployment | **NEW** | v2-new per ADR-0006 §1-§3. Append-only, lifecycle-gated deployment inventory: package, state, target cluster/env, image digest, run mode, actor, operation, outcome. Tier-1. No v1 equivalent (v1 deployment was out-of-band). |
| (none) | governance.deployment_current (view) | **NEW** | v2-new. Current-state projection over append-only governance.deployment: what is running where. ADR-0006 Consequences. |
| governance.lifecycle_state (enum) | governance.lifecycle_state (enum) | **KEEP** | Members carried verbatim (draft, candidate, staging, shadow, challenger, champion, deprecated). Consumed here to gate deployment. Type is shared/declared-once across the schema; this domain references it (and gates placement on it) but does not own it. |
| governance.deployment_channel (enum) | governance.deployment_environment / environment_kind | **DEFER** | v1 deployment_channel (development/staging/shadow/evaluation/production) tagged a run channel on the entity_version row, not a deployable cluster/environment. ADR-0006 replaces the placement concept with the environment/cluster registries + the state->environment matrix. Whether deployment_channel itself survives on the version row is owned by the REGISTRY/RUNTIME domain; deferred there to avoid double-mapping. |
| governance.user (AUTH domain) | FK targets: deployment.actor_user_id, harness_image.created_by, package.created_by, package_harness_image.created_by | **DEFER** | Actor/creator columns reference governance.user owned by the AUTH domain. FK constraints (fk_deployment_actor, fk_harness_image_created_by, etc.) are intentionally not emitted here; add them when the AUTH domain DDL lands to keep this fragment independently loadable. |
| governance.agent_version / governance.task_version (REGISTRY) | package.source_version_id (+ source_kind) | **DEFER** | package.source_version_id is a polymorphic FK to agent_version/task_version owned by the REGISTRY domain. Enforced via source_kind CHECK + app/control-plane validation here; a hard FK (or two nullable typed FKs) is deferred to the REGISTRY domain integration. |

## reporting

| v1 object | v2 object | Disposition | Note |
|---|---|---|---|
| compliance.embedding_config (table) | compliance.embedding_config | **CHANGE** | PK id->embedding_config_id (uuidv7). Made append-only: dropped mutable is_current boolean + partial-unique 'one current'; current row is latest by created_at, projected via embedding_config_current view (ADR-0005 rule 3). runtime free-text default 'fastembed' -> compliance.embedding_runtime enum. Added uq(model_name,model_version) natural key and ck dim>0. |
| compliance.embedding_config.is_current (column) | (dropped) compliance.embedding_config_current view | **DROP** | DROP-with-reason: mutable current flag is an in-place state flip; replaced by append-only + latest-by-created_at view. No capability loss: 'the single current embedding config' is still answerable. |
| compliance.embedding_config_one_current (partial unique index) | embedding_config_current view (LIMIT 1) | **CHANGE** | Uniqueness of 'current' now enforced by the projection (latest row), not a mutable-flag index. |
| analytics.mart_field (table) | analytics.mart_field | **CHANGE** | PK id->mart_field_id (uuidv7). semantic_type CHECK -> analytics.mart_field_semantic_type enum (members verbatim). FK embedding_model_id->embedding_config now named fk_mart_field_embedding_config ON DELETE SET NULL. uq/ix renamed to convention. created_at/updated_at timestamptz. |
| analytics.feed_view (table) | analytics.feed_view | **CHANGE** | PK id->feed_view_id (uuidv7). uq(view_name) named uq_feed_view_view_name. Tier-1; serves the ADR-0007 logical-mart read seam. |
| compliance.requirement_evidence_field (table) | compliance.requirement_evidence_field | **CHANGE** | PK id->requirement_evidence_field_id (uuidv7). role/aggregation CHECKs -> analytics.evidence_field_role + analytics.evidence_field_aggregation enums (verbatim). FKs renamed; canonical_requirement_id retargets the KEPT canonical center (ADR-0008). Added ck: aggregation only on measure role. Manifest survives the canonical->feature drop because it binds to canonical requirements, not features. |
| compliance.report_definition (table) | compliance.report_definition | **CHANGE** | PK id->report_definition_id (uuidv7). report_kind CHECK -> compliance.report_kind enum. uq(code), ck output_formats non-empty added. scope_params jsonb kept (genuinely open shape). |
| compliance.report_requirement (table) | compliance.report_requirement | **CHANGE** | PK id->report_requirement_id; report_id->report_definition_id FK. Named uq/fk/ix. canonical_requirement_id FK ON DELETE RESTRICT to KEPT canonical center. |
| compliance.report_field_override (table) | compliance.report_field_override | **CHANGE** | PK id->report_field_override_id; report_id->report_definition_id. role_override/aggregation_override CHECKs -> shared enums. Added ck_at_least_one (override must set >=1 attribute) as a real invariant. |
| compliance.report_sql_template (table) | compliance.report_sql_template | **CHANGE** | PK id->report_sql_template_id; 1:1 with report_definition via uq. referenced_mart_fields uuid[] removed from this table (normalized out). |
| compliance.report_sql_template.referenced_mart_fields (uuid[] column) | compliance.report_sql_template_field (NEW bridge) | **CHANGE** | Normalized the uuid[] catch-all array into a real FK bridge (ADR-0005 rule 2: no array where a relation belongs). Each referenced mart_field is a row with a real FK to analytics.mart_field. |
| compliance.report_run_log (table) | compliance.report_run_log | **CHANGE** | PK id->(report_run_log_id, created_at) for partitioning. Tagged Tier-2 append-only, RANGE-partitioned monthly on created_at, BRIN on created_at (ADR-0004 §8). status CHECK -> compliance.report_run_status enum. v1 mutated status/completed_at in place; v2 appends state events keyed by new run_uuid and projects latest via report_run_current. Added ck failed-has-error and ck completed>=created. |
| compliance.report_sql_template_field | compliance.report_sql_template_field | **NEW** | V2-NEW normalization bridge for BYO-SQL referenced mart fields (see referenced_mart_fields mapping). |
| compliance.embedding_config_current | compliance.embedding_config_current | **NEW** | V2-NEW current-state view: the single current embedding config. |
| compliance.report_run_current | compliance.report_run_current | **NEW** | V2-NEW current-state view over append-only report_run_log (latest per run_uuid). |
| analytics.mart_field_semantic_type / evidence_field_role / evidence_field_aggregation / report_kind / report_run_status / embedding_runtime | (same enum types) | **NEW** | V2-NEW Postgres enum types promoted from v1 inline CHECK-on-text pseudo-enums (members verbatim), per ADR-0005 rule preferring enum types over CHECK-on-text. |
| compliance.canonical_requirement (FK target) | compliance.canonical_requirement | **DEFER** | Referenced by requirement_evidence_field and report_requirement. Canonical center is KEPT (ADR-0008) but its DDL is owned by the COMPLIANCE control/evidence domain; defined there, not in this domain's section. |
| compliance.feature_plane / feature_capability / feature / requirement_feature_link (tables) | (controls + evidence model) | **DROP** | DROP-with-reason (ADR-0008): the v1 canonical->feature hierarchy + requirement_feature_link bridge are replaced by controls/evidence. Out of this domain's scope to redefine; the analytics manifest (requirement_evidence_field) intentionally binds canonical requirements -> mart_field, NOT features, so it carries forward cleanly. Drop owned by COMPLIANCE domain. |
| compliance.requirement_coverage / regulatory_framework / regulatory_provision / canonical_requirement_theme / provision_requirement_map (tables) | compliance control/evidence model | **DEFER** | Compliance-axis tables (ADR-0008 three-axis model) owned by the COMPLIANCE domain agent; not part of REPORTING & ANALYTICS. Deferred to that domain's section. |
| analytics.v_model_invocation_cost / analytics.execution_run_current (views) | logical-mart views | **DEFER** | Logical-mart/runtime views owned by RUNTIME/EXECUTION domain; mart_field/feed_view reference them by name but their DDL is defined in that domain. |


## validation / testing (C9 — added 2026-05-31)

| v1 object | v2 object | Disposition | Note |
|---|---|---|---|
| governance.gt_dataset_status (enum, 5 members) | governance.gt_dataset_status (enum, 5 members) | **KEEP** | Carried verbatim (collecting/labeling/adjudicating/ready/deprecated). Owned by this validation domain. |
| governance.gt_quality_tier (enum, 2 members) | governance.gt_quality_tier (enum, 2 members) | **KEEP** | Verbatim (silver/gold). |
| governance.gt_source_type (enum, 3 members) | governance.gt_source_type (enum, 3 members) | **KEEP** | Verbatim (document/submission/synthetic). |
| governance.gt_annotator_type (enum, 3 members) | governance.gt_annotator_type (enum, 3 members) | **KEEP** | Verbatim (human_sme/llm_judge/adjudicator). |
| test_case_mock.mock_kind / ground_truth_record_mock.mock_kind (VARCHAR CHECK) | governance.mock_kind (enum) | **CHANGE** | Free-text CHECK ('tool','source','target') promoted to a shared named enum per naming-conventions §5/§9; reused by both mock tables. |
| validation_run.status (VARCHAR DEFAULT 'running') | governance.validation_run_status (enum) | **CHANGE** | Free-text status comment (running/complete/failed) promoted to enum. |
| validation_record_result.match_type (VARCHAR) | governance.validation_match_type (enum) | **CHANGE** | Free-text (exact/partial/fuzzy) promoted to enum. |
| field_extraction_config.field_type (VARCHAR) | governance.extraction_field_type (enum) | **CHANGE** | Free-text (string/numeric/date/boolean/enum) promoted to enum. |
| field_extraction_config.match_type (VARCHAR) | governance.extraction_match_type (enum) | **CHANGE** | Free-text (exact/numeric_tolerance/case_insensitive/contains) promoted to enum. |
| field_extraction_config.tolerance_unit (VARCHAR) | governance.tolerance_unit (enum) | **CHANGE** | Free-text (percent/absolute) promoted to enum; now required when match_type='numeric_tolerance'. |
| incident.severity (VARCHAR NOT NULL) | governance.incident_severity (enum) | **CHANGE** | Free-text promoted to enum (critical/high/medium/low); member set is reviewer-confirmable against v1 callers (open issue). |
| incident.status (VARCHAR DEFAULT 'open') | governance.incident_status (enum) | **CHANGE** | Free-text promoted to enum (open/investigating/mitigated/resolved/closed); reviewer to confirm vocabulary. |
| evaluation_run.evaluation_type (VARCHAR NOT NULL) | governance.evaluation_type (enum) | **CHANGE** | Free-text promoted to enum (shadow/challenger/periodic/drift_check); reviewer to confirm vocabulary (open issue). |
| model_card.lifecycle_state (VARCHAR DEFAULT 'draft') | governance.model_card_state (enum) | **CHANGE** | Free-text promoted to a distinct card-review enum (draft/in_review/approved/superseded); deliberately separate from governance.lifecycle_state (entity lifecycle) to avoid conflation. |
| platform_settings.input_type (TEXT DEFAULT 'text') | governance.setting_input_type (enum) | **CHANGE** | Free-text (text/select/number) promoted to enum; select inputs now require options (CHECK). |
| governance.test_suite (table) | governance.test_suite (table) | **CHANGE** | Tier-1 SoR. id->test_suite_id (uuidv7), uuid_generate_v4->uuidv7, VARCHAR->text, created_by VARCHAR->created_by_user_id (soft ref to account_user), entity_id soft ref to agent/task (no DB FK), named PK/CHECKs, entity_kind & name-not-blank CHECKs added. suite_type kept open text (reviewer may enum). |
| governance.test_case (table) | governance.test_case (table) | **CHANGE** | Tier-1 SoR. id->test_case_id, suite_id->test_suite_id with named hard FK fk_test_case_test_suite ON DELETE CASCADE (owned child). metric_type reuses governance.metric_type enum. applies_to/excludes_versions kept as uuid[] soft refs. gin index on tags. name-not-blank CHECK. |
| governance.test_case_mock (table) | governance.test_case_mock (table) | **CHANGE** | Tier-1 SoR. id->test_case_mock_id, hard FK CASCADE to test_case, mock_kind CHECK->governance.mock_kind enum, call_order>=1 CHECK. v1 idx_tcm_kind -> ix_test_case_mock_test_case_kind. |
| governance.test_execution_log (table) | runtime.test_execution_log (table) | **CHANGE** | RELOCATED governance->runtime as Tier-2 append-only measurement log (ADR-0004). id->test_execution_log_id, suite_id/test_case_id/entity_version_id become soft refs (Tier-1->Tier-2, no DB FK), metric_type/channel reuse governance enums, BRIN on run_at, duration-nonneg CHECK. THIS IS THE SOURCE for analytics.v_validation_result (resolves C10). |
| governance.ground_truth_dataset (table) | governance.ground_truth_dataset (table) | **CHANGE** | Tier-1 SoR. id->ground_truth_dataset_id, created_by VARCHAR->created_by_user_id soft ref, entity_id/designed_for_version_id soft refs, superseded_by->superseded_by_dataset_id self-FK ON DELETE SET NULL, uq carried (entity_type,entity_id,name,version), count-nonneg & iaa-range CHECKs added. Curated rollup counters keep updated_at (bounded operational mutation), so SoR not event table. |
| governance.ground_truth_record (table) | governance.ground_truth_record (table) | **CHANGE** | Tier-1 SoR. id->ground_truth_record_id, dataset_id->ground_truth_dataset_id hard FK CASCADE, source_type reuses gt_source_type, uq(dataset,record_index) carried, gin on tags. |
| governance.ground_truth_record_mock (table) | governance.ground_truth_record_mock (table) | **CHANGE** | Tier-1 SoR. id->ground_truth_record_mock_id, record_id->ground_truth_record_id hard FK CASCADE, mock_kind CHECK->enum, call_order>=1, v1 idx_gtrm_kind -> composite index. |
| governance.ground_truth_annotation (table) | governance.ground_truth_annotation (table) | **CHANGE** | Tier-1 SoR. id->ground_truth_annotation_id, hard FKs CASCADE to record+dataset, v1 chk_human_fields/chk_llm_fields renamed ck_*_attribution and labeled_by VARCHAR->labeled_by_user_id soft ref, judge_prompt_version_id v1 HARD FK->prompt_version DROPPED to soft ref (cross-domain), confidence-range CHECK. V2-NEW: partial unique index uq_ground_truth_annotation_authoritative_per_record enforces 'exactly one authoritative per record' at DB level (v1 was app-only). |
| governance.validation_run (table) | runtime.validation_run (table) | **CHANGE** | RELOCATED governance->runtime as Tier-2 append-only. id->validation_run_id, dataset_id->ground_truth_dataset_id soft ref (Tier-1->Tier-2), status VARCHAR->governance.validation_run_status enum, run_by/sme_reviewed_by VARCHAR->user-id soft refs, entity_version_id soft ref, BRIN on run_at, entity_kind CHECK. |
| governance.validation_record_result (table) | runtime.validation_record_result (table) | **CHANGE** | RELOCATED governance->runtime Tier-2. id->validation_record_result_id, validation_run_id HARD FK CASCADE (same schema/tier owned child), ground_truth_record_id & decision_log_id soft refs (v1 already FK-free on decision_log_id), match_type VARCHAR->enum, uq(run,record_index) carried as uq_validation_record_result_run_index, duration/match_score range CHECKs. |
| governance.evaluation_run (table) | runtime.evaluation_run (table) | **CHANGE** | RELOCATED governance->runtime Tier-2 append-only. id->evaluation_run_id, evaluation_type VARCHAR->governance.evaluation_type enum, entity_version_id/champion_version_id soft refs, period-order & count-nonneg CHECKs, BRIN on created_at. promotion_recommendation kept open text (reviewer may enum). |
| governance.model_card (table) | governance.model_card (table) | **CHANGE** | Tier-1 SoR. id->model_card_id, lifecycle_state VARCHAR->governance.model_card_state enum (distinct from entity lifecycle), validated_by/approved_by VARCHAR->user-id soft refs, validation_run_id v1 HARD FK->soft ref (Tier-1->Tier-2), entity_version_id soft ref, V2-NEW uq(entity_type,entity_version_id,card_version), card_version>=1 & entity_kind CHECKs. |
| governance.metric_threshold (table) | governance.metric_threshold (table) | **CHANGE** | Tier-1 SoR. id->metric_threshold_id, materiality_tier reuses governance enum, entity_id soft ref, VARCHAR->text. v1 uq_threshold had NULLable field_name in key (broken uniqueness for aggregate rows) -> split into two partial unique indexes (aggregate WHERE field_name IS NULL, per-field WHERE field_name IS NOT NULL). minimum<=target CHECK added. |
| governance.field_extraction_config (table) | governance.field_extraction_config (table) | **CHANGE** | Tier-1 SoR. id->field_extraction_config_id, field_type/match_type/tolerance_unit VARCHAR->enums, entity_id soft ref, v1 task-only CHECK carried as ck_*_task_only, uq(entity_type,entity_id,field_name) carried, V2-NEW CHECK requiring tolerance_unit when match_type='numeric_tolerance'. |
| governance.incident (table) | governance.incident (table) | **CHANGE** | Tier-1 SoR (curated workflow, bounded mutation). id->incident_id, severity/status VARCHAR->enums, rollback_approved_by VARCHAR->rollback_approved_by_user_id soft ref, entity/version/rollback_to_version/affected_context_ids soft refs, title-not-blank/count-nonneg/rollback-target-when-executed CHECKs. |
| governance.description_similarity_log (table) | runtime.description_similarity_log (table) | **CHANGE** | RELOCATED governance->runtime Tier-2 append-only. id->description_similarity_log_id, flagged GENERATED ALWAYS AS (similarity_score>0.85) STORED carried verbatim, reviewed_by VARCHAR->reviewed_by_user_id soft ref, both entity refs soft, score-range CHECK, BRIN on checked_at. resolution kept open text. |
| governance.platform_settings (table) | governance.platform_settings (table) | **CHANGE** | Tier-1 SoR, intentionally MUTABLE config (not append-only). v1 natural PK `key` (bare/reserved-ish) -> surrogate platform_settings_id (uuidv7) + UNIQUE setting_key (renamed for reserved-word hygiene §1/§3); value->setting_value; input_type TEXT->governance.setting_input_type enum; key-not-blank CHECK; select-requires-options CHECK. |
| governance.entity_type (+test_suite,+ground_truth_dataset members) | governance.entity_type (ENTITIES domain) | **DEFER** | This domain REFERENCES governance.entity_type and relies on its test_suite/ground_truth_dataset members. The enum is OWNED by the ENTITIES domain (02-entities.sql already carries both members). Confirms ASSEMBLY C11: members are now backed by real tables in this domain (test_suite, ground_truth_dataset), so they should survive. |
| governance.metric_type / materiality_tier / deployment_channel (enums) | same (ENTITIES domain) | **DEFER** | Referenced by test_case/test_execution_log (metric_type), metric_threshold (materiality_tier), test_execution_log (deployment_channel). Owned/declared by ENTITIES domain (02-entities.sql); referenced here, not re-declared (avoids duplicate-object on assembly). |
| analytics.v_validation_result (deferred view) | analytics.v_validation_result (view) | **NEW** | Resolves ASSEMBLY C10. Reference projection over runtime.test_execution_log (now defined). Canonical ownership is the analytics-mart domain at assembly time; emitted here to demonstrate the source dependency is satisfied (C9->C10 chain closed). |
| queries/testing.sql named queries (list/insert/delete + backward-compat tool_name aliases) | (application query layer; re-pointed to renamed columns) | **DEFER** | No schema object. All 30+ named queries map onto the renamed PKs/columns (id->*_id, suite_id->test_suite_id, mock_key alias tool_name preserved, dataset_id->ground_truth_dataset_id). The 'list_all_test_suites'/'list_all_ground_truth_datasets' joins to agent/task become app-side joins across domains (soft refs). Behavior preserved; query rewrite owned by the application layer. |
