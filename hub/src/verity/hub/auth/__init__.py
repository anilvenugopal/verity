"""Identity & authorization (user-authentication.md).

Built against the canonical schema (core.actor / account_user / actor_role_grant /
current_actor_role, audit.auth_event). The Entra OIDC flow is stubbed behind the same
Authenticator interface as the fully-implemented, fail-closed mock path (FR-030).
"""
