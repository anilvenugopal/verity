# Contributing to hub/portal

See [hub/docs/dev-setup.md](../docs/dev-setup.md) for the full development setup guide, including:

- How to run Vitest tests (`./dev test:portal` or `npm run test`)
- Node version pinning (`.nvmrc`)
- The `noUncheckedIndexedAccess` TypeScript rule — **do not revert this**
- VSCode Testing tab configuration
